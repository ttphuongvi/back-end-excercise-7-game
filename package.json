{
  "name": "back-end-exercise-7-game",
  "version": "1.0.0",
  "description": "",
  "main": "fakeData.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm i && json-server --watch db.json --port 3001"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "faker": "^5.5.3",
    "json-server": "0.16.3"
  }
}