{
  "name": "back-end-exercise-7-game",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon main.js",
    "build": "npm i && echo \"success build\" && node main.js",
    "start": "node main.js",
    "build-json": "json-server --watch db.json --port 3001"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "faker": "^5.5.3",
    "json-server": "0.16.3"
  },
  "devDependencies": {
    "nodemon": "2.0.19"
  }
}